<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}Devour Time!{{else}}Throw up!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">Eat!</button>
</li>
